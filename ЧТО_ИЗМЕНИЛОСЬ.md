# Фикс: синхронный вызов в POST /api/portals ломался на туннеле Gateway

## Изменённые файлы (пути — как в вашем репозитории)

```
server/app/db.py                          — статус 'connecting' + миграция схемы
server/app/repositories/external_portals.py — list_connecting_portals, mark_portal_active
server/app/poller.py                      — finish_connecting_portal, schedule_finish_connecting
server/app/routes/portals.py              — POST больше не делает исходящих вызовов
server/tests/test_stage3_portals.py       — тесты переписаны под новый флоу
server/widget/index.html                  — статус 'connecting' в UI + автополлинг
widget/index.html                         — та же правка (см. примечание ниже)
```

## Суть фикса

Раньше `POST /api/portals` синхронно проверял credentials и создавал чат
прямо в обработчике запроса — это стабильно рвалось на уровне туннеля
Gateway (см. `Контекст.txt`).

Теперь:
1. Запрос сразу сохраняет портал со статусом `connecting` и отвечает 201 —
   никаких исходящих вызовов в этом обработчике больше нет.
2. Проверка credentials и создание чата переехали в
   `poller.finish_connecting_portal()`, который запускается:
   - сразу, отдельной asyncio-задачей (`schedule_finish_connecting`),
     для быстрого отклика в UI;
   - и подстраховкой — на каждом цикле фонового опроса (`poll_once`),
     на случай если сервер перезапустится раньше, чем задача успеет
     завершиться.
3. Виджет узнаёт результат, опрашивая `GET /api/portals` каждые 2.5 сек,
   пока статус `connecting` не сменится на `active`/`error`.

## Важно перед деплоем

**Миграция БД.** Файл БД на сервере (`/data`) переживает передеплой, а
`CREATE TABLE IF NOT EXISTS` не трогает уже существующую таблицу — значит
старый `CHECK`-констрейнт без статуса `connecting` остался бы в силе, и
вставка новых записей начала бы падать. В `db.py` добавлена миграция
(`_migrate_connecting_status`), которая при старте сама пересоздаёт таблицу
с новым constraint'ом и переносит все существующие строки. Она безопасна и
идемпотентна — но всё равно рекомендую сделать бэкап `/data/app.db` перед
первым деплоем после этого фикса, на всякий случай.

## Про два widget/index.html

Обнаружил, что реально раздаётся `server/widget/index.html` (это видно по
`WIDGET_INDEX_PATH` в `main.py`) — в нём уже была ваша retry-логика на
холодный старт Gateway (`fetchWithRetry`, `BH_APP_STARTING`). Корневой
`widget/index.html`, похоже, более старая копия без неё. Я применил фикс
в обе, но стоит решить, нужна ли вам вторая копия вообще, или это можно
удалить, чтобы не путаться в будущем.

## Тесты

Все 29 тестов (по 4 файлам, каждый — отдельным запуском, как и раньше
рекомендует ваш README) проходят:
```
test_stage3_portals.py   8 passed
test_stage4_poller.py    6 passed
test_stage5_forwarding.py 10 passed
test_stage6_widget.py    5 passed
```
При запуске `pytest tests/` всеми файлами разом два теста в stage3 падают
из-за коллизии `DATABASE_URL` между файлами (каждый файл переопределяет
переменную окружения на этапе импорта) — это существовало и до фикса,
не связано с этой правкой.

## Обновление (после первого реального теста на tarangroup.bitrix24.ru)

Вебхук оказался рабочим (проверили напрямую через `.../profile.json`), но
портал всё равно ушёл в `error` — а в личном кабинете Вайбкода не оказалось
логов приложения (только журнал HTTP-доступа), так что понять причину было
нечем.

Добавил колонку `error_message` в `external_portals` (миграция через
`ALTER TABLE ADD COLUMN` — безопасна, ничего не пересоздаёт) и теперь
`finish_connecting_portal` / `_process_portal` сохраняют туда точный текст
ошибки (включая `type(exc).__name__` для неожиданных исключений). Виджет
показывает этот текст вместо статичной подсказки «Ключ или вебхук больше не
действует» — теперь причина видна прямо в UI, логи сервера не нужны.

Заодно расширил обработку ошибок в `finish_connecting_portal` вокруг
`create_group_chat` — раньше ловился только `VibeApiError`, а из PLAN.md
известно, что контракт `POST /v1/chats` был проверен только по документации
и в мокнутых тестах, но НЕ на реальном вызове. Если реальный ответ API
отличается от ожидаемого (другой формат `data`, другой ключ и т.п.), раньше
это тихо ронялось необработанным исключением внутри asyncio-задачи без
всякого следа. Теперь любое исключение на этом шаге тоже ловится и его текст
попадает в `error_message` — так что при следующей попытке подключения вы
увидите точную причину прямо в виджете.

**Дополнительно изменённые файлы в этом обновлении:**
```
server/app/db.py                       — + миграция error_message
server/app/repositories/external_portals.py — + mark_portal_error, поле error_message
server/app/poller.py                   — оба места установки status=error теперь пишут причину
server/app/routes/portals.py           — error_message в API-ответе
server/widget/index.html, widget/index.html — показывают error_message вместо статичного текста
server/tests/test_stage4_poller.py     — тест обновлён под mark_portal_error
```

Все 29 тестов проходят (по отдельным файлам).

**Что делать дальше:** задеплойте это обновление, затем нажмите
«Отключить» у текущей записи tarangroup.bitrix24.ru (она осталась в статусе
error без текста причины — это запись ДО миграции) и подключите портал
заново тем же вебхуком. Теперь при любом сбое вы увидите точный текст
ошибки прямо под статусом в виджете — пришлите мне его, если снова не
получится, и я смогу сразу сказать, в чём дело.


## Обновление 3: найдена настоящая причина TOKEN_MISSING

Вебхук внешнего портала и вся асинхронная логика оказались в порядке.
Реальная причина — в типе ключа основного портала. `VIBE_APP_KEY` (ключ
авторизации, `vibe_app_...`) — это ключ **от лица конкретного
пользователя**: каждый запрос, кроме `/v1/me`, `/v1/guide` и `/v1/oauth/*`,
требует `Authorization: Bearer <сессионный токен>`. А такой токен живёт
24 часа **без автопродления** — получить его без интерактивного входа
пользователя в браузере нельзя. Для фонового опроса раз в ~45 сек
(`poller.py`) это физически не работает: за экраном никого нет, продлевать
сессию некому.

Официальная документация Вайбкода прямо описывает это как отдельный
сценарий и рекомендует для него **личный API-ключ** (`vibe_api_...`) —
он работает по одному `X-Api-Key`, вообще без Bearer:
https://vibecode.bitrix24.tech/docs/keys-auth (раздел «Передача ключа»,
абзац про фоновые сценарии).

### Что изменилось в коде

```
server/app/config.py      — новая настройка vibe_api_key (+ подробный
                             комментарий, почему их два)
server/app/vibe_client.py — create_group_chat и send_chat_message теперь
                             берут settings.vibe_api_key вместо
                             settings.vibe_app_key
server/.env.example       — задокументирована новая переменная VIBE_API_KEY
```

**`VIBE_APP_KEY` никуда не делся** — он по-прежнему нужен для встраивания
виджета в интерфейс Битрикс24 и определения личности сотрудника через
Gateway (`app/auth.py`, `get_me`). Меняется только то, каким ключом
создаётся чат и отправляются сообщения.

### Что нужно сделать вам ПЕРЕД деплоем

1. Зайдите в личный кабинет Вайбкода → раздел **«Ключи API»** (это ДРУГОЙ
   раздел, не «Ключи авторизации», где вы создавали `VIBE_APP_KEY`).
2. Создайте новый ключ. Скоупы Битрикс24 — `imbot` (см. Обновление 6 ниже —
   после перехода на Бот-платформу просто `im` уже недостаточно). Скоупы
   Вайбкод можно все снять, они не нужны для этой задачи.
3. Скопируйте выданный ключ (`vibe_api_...`) — показывается один раз.
4. Добавьте в `.env` на сервере (там же, где остальные переменные) новую
   строку:
   ```
   VIBE_API_KEY=vibe_api_...
   ```
5. Задеплойте обновление.
6. Отключите текущую запись `tarangroup.bitrix24.ru` и подключите заново.

Все 29 тестов проходят (по отдельным файлам).

## Обновление 4: исправлено имя переменной (коллизия с deploy_backend.py)

Спасибо за вопрос про переменные окружения — он вскрыл мою ошибку. Я назвал
новую переменную `VIBE_API_KEY`, но это имя уже занято: `scripts/deploy_backend.py`
использует `VIBE_API_KEY` в shell-окружении для СОВСЕМ другого ключа (личный
ключ со скоупом `vibe:infra`, нужен только локально в момент самого деплоя,
никогда не кладётся в `.env`). Хуже того — под старым именем переменная не
попала бы в список `ENV_VARS_TO_FORWARD`, который решает, что из `.env`
реально прокидывается в контейнер на сервере — то есть даже при правильном
значении в `.env` до приложения она бы не доехала.

**Переименовал:** `VIBE_API_KEY` → `VIBE_BACKGROUND_API_KEY` (в `.env`, `config.py`,
`vibe_client.py`) и добавил её в `ENV_VARS_TO_FORWARD` в `deploy_backend.py`.

### Итоговый список переменных

**В `server/.env` (пробрасываются в контейнер на деплое):**
```
VIBE_APP_KEY              — ключ авторизации, для виджета и Gateway-сессий
VIBE_BACKGROUND_API_KEY   — НОВЫЙ, личный ключ (vibe_api_...), для создания
                             чата и отправки сообщений (скоуп imbot — см.
                             Обновление 6)
VIBE_API_BASE_URL
APP_BASE_URL
PLACEMENT_TITLE
DATABASE_URL
CREDENTIALS_ENCRYPTION_KEY
```

**Только в shell-сессии перед запуском `python -m scripts.deploy_backend`
(НЕ в `.env`):**
```
VIBE_API_KEY       — личный ключ, скоуп vibe:infra, для создания/деплоя
                      самого сервера (используется только скриптом деплоя)
DEPLOY_SERVER_ID    — если редеплоите существующий сервер (не создаёте новый)
```

Все 29 тестов проходят.

## Обновление 5: правки по пересылке из открытых линий

По скриншоту с реальным тестом открытой линии нашлись два бага:

**1. Системные события пересылались как сообщения клиента.**
«Интеллектик Сервисный Аккаунт изменил название чата» выглядело как будто
это написал «Клиент открытой линии #0». Официальная документация Битрикс24
подтверждает: `senderid: "0"` в ответе `imopenlines.session.history.get` —
это как раз признак служебного события (в их примере — «Создан новый лид»),
а не сообщение реального клиента. Добавил фильтр в
`external_message_fetcher.py`: такие записи больше не пересылаются.

**2. Ссылка «Открыть диалог» разворачивалась в большую карточку.**
Голый URL на bitrix24-домене Битрикс24 сам разворачивает в крупную карточку
с логотипом и кнопкой «Войти в Битрикс24» — на каждое пересланное сообщение.
Обернул ссылку в BB-код `[url=...]Открыть диалог[/url]` (`poller.py`,
`_format_digest`) — по документации Битрикс24 это стандартный способ дать
кликабельную ссылку с текстом без авто-разворачивания.

Разные номера клиента («#86», потом «#1756») — НЕ баг: похоже на поведение
тестового виджета открытой линии, который создаёт нового анонимного
посетителя (лид/контакт) на каждый новый тестовый сеанс. Реальные клиенты с
сайта будут сохранять один и тот же ID в рамках диалога.

### Изменённые файлы
```
server/app/external_message_fetcher.py — фильтр senderid == "0"
server/app/poller.py                   — BB-код вместо голой ссылки
server/tests/test_stage4_poller.py     — новый тест на фильтр
server/tests/test_stage5_forwarding.py — тест на формат ссылки
```

Все 30 тестов проходят (было 29, добавился один новый).

**Осталось проверить:** личные сообщения (вы упомянули, что ещё не
тестировали) — по коду путь другой (`im.dialog.messages.get`, не
`imopenlines.*`), так что стоит проверить отдельно.

## Обновление 6: переход на Бот-платформу — чтобы сообщения были непрочитанными

Раньше `create_group_chat`/`send_chat_message` ходили в общие эндпоинты
`/v1/chats` (`im.chat.add`/`im.message.add`) — сообщения отправлялись от
лица **владельца личного ключа**, то есть, скорее всего, того же
сотрудника, который смотрит на виджет. Такое сообщение физически не может
быть непрочитанным для самого себя — отсюда галочки "прочитано" на
скриншоте.

Переписал `vibe_client.py` на Бот-платформу Вайбкода (`imbot.v2.*`,
`/v1/bots/...`): бот — отдельная сущность в Битрикс24 (отдельный user с
`bot=true`), и сообщения от его имени приходят участнику как обычные новые
сообщения, корректно помечаются непрочитанными. Подробности:
https://vibecode.bitrix24.tech/docs/bots

### Как это работает

- Бот регистрируется один раз (`POST /v1/bots`, фиксированный `code`) и
  дальше переиспользуется — `botId` кэшируется в памяти процесса.
- При перезапуске сервера повторная регистрация с тем же `code` вернёт
  `409 BOT_ALREADY_EXISTS`, из тела которого платформа сама отдаёт `botId`
  уже существующего бота — это задокументированное поведение, не хак,
  так что отдельно хранить `botId` в БД не нужно.
- `create_group_chat` → `POST /v1/bots/:botId/chats` (`imbot.v2.Chat.add`)
- `send_chat_message` → `POST /v1/bots/:botId/messages` (`imbot.v2.Chat.Message.send`)

### ВАЖНО: скоуп ключа меняется

`VIBE_BACKGROUND_API_KEY` теперь требует скоуп **`imbot`** (Бот-платформа),
а не `im`, как я говорил раньше — с одним `im` регистрация бота и создание
чата вернутся `403 SCOPE_DENIED`. Если вы уже создали ключ только со
скоупом `im`, **зайдите в личный кабинет Вайбкода и добавьте `imbot`** к
существующему ключу (или создайте новый — тогда придётся обновить значение
`VIBE_BACKGROUND_API_KEY` в `.env`).

### Изменённые файлы
```
server/app/vibe_client.py — create_group_chat/send_chat_message переехали
                             на Бот-платформу, добавлена регистрация бота
server/app/config.py      — комментарий про скоуп imbot
server/.env.example       — комментарий про скоуп imbot
```

## Обновление 7: собственные сообщения сотрудника — сразу прочитанные

По вопросу «а можно пересылать мои сообщения от моего имени, а клиента —
через бота»: отправлять от вашего имени в фоне невозможно (та же проблема
с сессионным токеном, из-за которой начался весь этот разбор — без вас за
экраном 24-часовую сессию `vibe_app_` продлевать некому).

Вместо этого: всё по-прежнему идёт через бота, но если дайджест диалога
**целиком** состоит из ваших собственных сообщений на внешнем портале —
он сразу помечается прочитанным (`POST /v1/bots/:botId/chats/:dialogId/read`,
`imbot.v2.Chat.Message.read`). Непрочитанными остаются только реальные
входящие от собеседника. Если в одном дайджесте смешаны и ваши сообщения,
и сообщения собеседника — весь дайджест остаётся непрочитанным (чтобы не
спрятать входящее от клиента).

**Ограничение:** работает только для порталов, подключённых через
**вебхук**. Для порталов с авторизацией через личный ключ (`vibe_api`) ваш
числовой ID на том портале через доступный эндпоинт не определить — там
поведение как раньше (все сообщения, включая ваши, непрочитанные).

### Изменённые файлы
```
server/app/db.py                       — колонка owner_external_user_id (+ миграция)
server/app/repositories/external_portals.py — поле в датаклассе, set_owner_external_user_id
server/app/poller.py                   — сохранение ID при подключении вебхука,
                                          пометка "своих" дайджестов прочитанными
server/app/external_message_fetcher.py — поле author_id в FetchedMessage
server/app/vibe_client.py              — mark_message_read
server/tests/test_stage4_poller.py     — обновлена фикстура _portal
server/tests/test_stage5_forwarding.py — обновлены фикстуры + 4 новых теста
```

## Обновление 8: способ авторизации vibe_api убран, остался только webhook

Раз `vibe_api` как способ подключения внешнего портала не давал определить
ID сотрудника (нужен для пометки "своих" сообщений прочитанными) и вообще
не был проверен вживую — убрал его полностью.

### Что изменилось
```
server/widget/index.html, widget/index.html
    — убран переключатель способа авторизации, осталось одно поле "URL вебхука"

server/app/routes/portals.py
    — ConnectPortalRequest.auth_type: Literal["webhook"] (было Literal["vibe_api", "webhook"])
      API теперь отклоняет запросы с auth_type=vibe_api как 422

server/app/repositories/external_portals.py
    — AuthType = Literal["webhook"]

server/app/db.py
    — CHECK на колонке auth_type НАМЕРЕННО оставлен разрешающим оба значения
      (сужение потребовало бы деструктивной миграции ради поля, которое
      больше никогда не будет писаться) — но новый код никогда не пишет 'vibe_api'

server/app/external_portal_client.py
    — validate_vibe_api_key() удалена целиком

server/app/external_message_fetcher.py
    — _vibe_batch_call() удалена, _call_method всегда идёт через вебхук

server/app/poller.py
    — finish_connecting_portal упрощена: всегда webhook-ветка

server/tests/test_stage3_portals.py
    — переписан: TestConnectPortalVibeApi удалён, его сценарии (шифрование
      credentials, невалидные credentials → error, ошибка создания чата →
      error) перенесены на webhook; добавлен новый тест
      test_vibe_api_auth_type_rejected_by_api (проверяет 422)
server/tests/test_stage6_widget.py
    — тест на переключатель способа авторизации заменён на тест "виджет
      предлагает только вебхук"
```

### Не забудьте
`README.md` и `PLAN.md` в корне проекта — это ваши собственные документы
планирования, я их не трогал, но там наверняка ещё описаны оба способа
авторизации (раздел 2 и другие) — стоит обновить самостоятельно, когда
будет время, чтобы не путаться в будущем.

## Обновление 9: мини-инструкция под полем вебхука

Под полем URL вебхука теперь есть короткая пошаговая подсказка: где в
портале-источнике создать вебхук, какие разделы отметить (`im` +
`imopenlines` — оба нужны: первый для обычных диалогов, второй для
открытых линий, см. external_message_fetcher.py) и не забыть скопировать
ссылку целиком со слэшем на конце.

### Изменённые файлы
```
server/widget/index.html, widget/index.html — новый текст подсказки + стили для списка
```
